// tab.js

// Listen for the 'update-tab' event and handle the user input
// ipcRenderer.on('search-client', (event, userInput) => {
//   // Handle the value, for example, update a DOM element
//   document.getElementById('tabContent').innerText = `Received user input: {userInput}`;
// });


// function show() {
//     document.getElementById('tabContent').innerText = 'aaaaaaaaaaaaaaaaa';
// }